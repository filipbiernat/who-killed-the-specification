Regenerate `src/coffee_machine/` from the requirements in `spec/`, following
the spec-to-code skill.

- The requirements are the source of truth. Implement every one as written.
  If one looks wrong, do not fix it and do not skip it: name it in your
  summary and let a person decide.
- Do not edit anything in `spec/` or `tests/`.
- Do not run the tests. Whoever wrote the code does not grade it; the person
  at the keyboard and CI do.
- Keep every name the visualisation reads; the skill lists them.

Finish with one line per requirement whose code changed: its UID and what the
code now does.
